export interface Movie {
    actors: Array<number>;
    duration: number;
    genre: Array<string>;
    id: number;
    imdbRating: number;
    poster: string;
    title: string;
    year: Number;
}